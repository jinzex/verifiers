name: Test

on:
  pull_request:
    branches: [main]
    paths:
      - "**.py"
      - "pyproject.toml"
      - "packages/**"
      - ".github/workflows/test.yml"
  push:
    branches: [main]
    paths:
      - "**.py"
      - "pyproject.toml"
      - "packages/**"
      - ".github/workflows/test.yml"

env:
  HF_HOME: ${{ github.workspace }}/.cache/huggingface

jobs:
  test:
    name: Verifiers
    runs-on: ubuntu-latest
    env:
      PRIME_API_KEY: ${{ secrets.PRIME_API_KEY }}
      PRIME_TEAM_ID: ${{ secrets.PRIME_TEAM_ID }}
      HF_TOKEN: ${{ secrets.HF_TOKEN }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Cache Hugging Face tokenizers
        uses: actions/cache@v4
        with:
          path: ${{ env.HF_HOME }}
          key: hf-tokenizers-${{ runner.os }}-${{ hashFiles('uv.lock', 'tests/test_renderer_client.py', 'tests/test_renderer_e2e.py') }}
          restore-keys: |
            hf-tokenizers-${{ runner.os }}-

      - name: Install dependencies
        run: uv sync

      - name: Install prime
        run: uv pip install prime

      - name: Run tests
        run: |
          uv run pytest tests/ -v --ignore=tests/v1 -m "not prime" --cov=verifiers --cov-report=xml --cov-report=term

      - name: Run v1 tests
        run: |
          uv run pytest tests/v1 -vv -n auto -m "not prime and not modal and not apptainer" --cov=verifiers --cov-append --cov-report=xml --cov-report=term
